fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
streamlit>=1.38
pandas>=2.2
requests>=2.32
python-dotenv>=1.0
pytest>=8.0
httpx>=0.27

# Añadir según el/los proveedor(es) externo(s) elegido(s) para la comparación:
# google-genai        # Gemini
# openai               # GPT
# anthropic            # Claude
# groq                 # Groq (capa gratuita)
